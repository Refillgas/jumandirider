import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0000FF); // Blue color from screenshots
  static const Color accent = Color(0xFF00FF00); // Green for accept button
  static const Color danger = Color(0xFFFF0000); // Red for reject button
  static const Color background = Colors.white;
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;
  static const Color divider = Color(0xFFE0E0E0);
}